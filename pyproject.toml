[project]
name = "bing-wallpaper-app-image-downloader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.7"
dependencies = [
    "requests>=2.0",
]
